Python -- 3.10.11
HOW TO RUN THE APP
the app is in the jupyter notebook under the headline "TASK 3",
just make sure all the requirments are installed(you can check it by activating the first cell in the notebook)
and run the app, the app should be on port 5000
here is an example of the URL:
http://127.0.0.1:5000/group?groups=5

in the commit you will find 'method1.json','method2.json','method3.json', those are the outputs of each method
have fun!
